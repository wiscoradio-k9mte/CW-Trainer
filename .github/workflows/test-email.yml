# TEMPORARY — one-off manual test of the escalation-email path. Remove after verifying.
name: Test Escalation Email
on:
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/notify-escalation.yml
    with:
      subject: "Test — escalation email check"
      body: |
        This is a one-off test of the CW Trainer escalation email.
        If you're reading this in your inbox, the MAIL_* secrets and the
        notify-escalation workflow are working. Safe to ignore.
      severity: "low"
    secrets: inherit
